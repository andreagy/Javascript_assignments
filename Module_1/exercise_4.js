'use strict';

const studentName = prompt("Give your name to the Sorting Hat");
let result = `${studentName}, now you are in the class.`
const sort = Math.floor(Math.random() * 4) +1;
switch (sort) {
  case
}